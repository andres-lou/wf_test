name: test

on:
  push:

  schedule:     
    - cron: '*/3 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: get cake
        env:
          CAKE: ${{ secrets.CAKE }}
        run: |
          echo $CAKE > cake
          python ~/wf_test/wf_test.py
